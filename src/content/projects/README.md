# Projects

One `.mdx` file per GitHub repository. The file name is the URL: `ray-tracing.mdx` → `/projects/ray-tracing/`.

The front-matter is validated by `src/content/schema.ts` — `npm run dev` / `npm run build` will stop and
tell you which file and field is wrong. Article order: Idea → Architecture → Implementation → Results → Lessons.
